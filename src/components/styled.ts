import styled from "styled-components";
import { Stack } from "react-bootstrap";

export const SingleIndent = styled.div`
    width: 30px
`
export const OriginalJsonContainer = styled.div`
    height: calc(100vh/3);
    overflow-y: scroll;
    margin-top: 30px;
`
